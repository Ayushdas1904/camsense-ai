# CamSense AI · AI service dependencies
# Foundation phase installs only the web layer. Heavy CV/ML libraries
# (opencv, ultralytics/YOLO, face recognition, torch) are commented out and
# added in Review 1/2 when the real models are integrated — this keeps the
# foundation lightweight and fast to install.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── Added in later reviews (do not install yet) ────────────────
# opencv-python-headless==4.10.0.84   # video I/O + frame handling
# numpy==1.26.4
# ultralytics==8.2.0                  # YOLOv8/YOLOv11 human & weapon detection
# torch==2.3.1                        # model backend
# face-recognition==1.3.0             # face embeddings (Review 2)
