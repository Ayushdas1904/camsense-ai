# Models

Trained model weights live here (e.g. `yolov8n.pt`, weapon-detection weights,
face-recognition models). They are added during Review 1 / Review 2 when real
detectors replace the foundation's mock implementations.

Weight binaries are git-ignored (see root `.gitignore`) — download or train
them per the instructions that ship with each review rather than committing
large files.
